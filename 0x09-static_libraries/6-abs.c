#include "main.h"

/**
 * _abs - function that computes absolute value of an integer
 * @a: the interger that is computed
 *
 * Return: absolute value of an integer
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
